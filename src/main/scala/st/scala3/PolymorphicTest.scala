package st.scala3

import cats.effect.kernel.Sync
import cats.effect.{ExitCode, IO, IOApp}

def transmit[F[_]: Sync]


object PolymorphicTest extends IOApp{
  override def run(args: List[String]): IO[ExitCode] = {

  }
}
