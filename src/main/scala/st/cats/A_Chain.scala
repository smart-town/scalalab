package st.cats

object A_Chain {
  def main(args: Array[String]): Unit = {
    println(
      """
        |Chain is an immutable sequence data structure that allows constant time prepending,
        |appending and concatenation. This makes it especially efficient when use as a Monoid,
        |
        |""".stripMargin)
  }
}
